# Example .env.development for vibe-share
# Set the backend API base URL (change to your backend's host:port)
VITE_API_BASE_URL=http://localhost:5000

# Optional: Explicit socket URL (falls back to VITE_API_BASE_URL if unset)
VITE_SOCKET_URL=http://localhost:5000

# Notes:
# - During development the Vite dev server runs on port 3000 (configured in vite.config.ts).
# - If your backend runs on a different port, update VITE_API_BASE_URL accordingly.
